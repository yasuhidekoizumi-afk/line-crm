'use client'

import { useEffect, useMemo, useState } from 'react'
import * as XLSX from 'xlsx'
import { useAccount } from '@/contexts/account-context'
import { fetchApi } from '@/lib/api'

type Address = { recipientName: string | null; postalCode: string | null; prefecture: string | null; addressLine1: string | null; addressLine2: string | null; phone: string | null; confirmedAt: string | null }
type Influencer = {
  friendId: string; displayName: string | null; instagramHandle: string | null; categories: string[]; followerBand: string | null
  contactEmail: string | null; contactPhone: string | null; ageGroup: string | null; gender: string | null
  giftingInterests: string[]; dietaryNotes: string | null; hasShopifyPurchase: boolean; isFollowing: boolean; profileCompletedAt: string | null; address: Address | null
}
type GiftingLog = { id: string; friendId: string; creatorName: string | null; instagramHandle: string | null; productName: string; productPageUrl: string | null; status: string; requestedAt: string | null; shippedAt: string | null; postPublishedAt: string | null; postType: string | null; postUrl: string | null; reach: number | null; impressions: number | null; likes: number | null; comments: number | null; saves: number | null; effectNotes: string | null }

function Field({ label, value }: { label: string; value: string | null | undefined }) {
  return <div><dt className="text-xs font-medium tracking-wide text-slate-500">{label}</dt><dd className="mt-1 break-words text-sm font-medium text-slate-900">{value || '—'}</dd></div>
}

function downloadInfluencers(items: Influencer[]) {
  const rows = items.map((item) => ({
    '表示名': item.displayName || '', 'Instagramアカウント': item.instagramHandle || '', '発信ジャンル': item.categories.join('・'),
    'フォロワー数': item.followerBand || '', 'メールアドレス': item.contactEmail || '', '連絡先電話番号': item.contactPhone || '',
    '年代': item.ageGroup || '', '性別': item.gender || '', '公式ショップ購入経験': item.hasShopifyPurchase ? 'あり' : 'なし',
    '興味のあるギフティング': item.giftingInterests.join('・'), 'アレルギー・避けたい食材': item.dietaryNotes || '',
    '配送先お名前': item.address?.recipientName || '', '郵便番号': item.address?.postalCode || '', '都道府県': item.address?.prefecture || '',
    '市区町村・町名・番地': item.address?.addressLine1 || '', '建物名・部屋番号': item.address?.addressLine2 || '',
    '配送先電話番号': item.address?.phone || '', 'プロフィール登録日時': item.profileCompletedAt || '',
  }))
  const sheet = XLSX.utils.json_to_sheet(rows)
  sheet['!cols'] = [14, 24, 24, 16, 30, 18, 10, 10, 18, 26, 32, 18, 12, 12, 32, 24, 18, 24].map((wch) => ({ wch }))
  const workbook = XLSX.utils.book_new()
  XLSX.utils.book_append_sheet(workbook, sheet, 'インフルエンサー一覧')
  XLSX.writeFile(workbook, `インフルエンサー登録情報_${new Date().toISOString().slice(0, 10)}.xlsx`)
}

const statusLabels: Record<string, string> = { requested: '依頼済み', accepted: '承諾', shipped: '発送済み', posted: '投稿済み', declined: '辞退', cancelled: '中止' }
const blankLog = { friendId: '', productName: '', productPageUrl: '', status: 'requested', requestedAt: '', shippedAt: '', postPublishedAt: '', postType: 'Instagram', postUrl: '', reach: '', impressions: '', likes: '', comments: '', saves: '', effectNotes: '' }

function GiftingHistory({ lineAccountId, creators }: { lineAccountId: string; creators: Influencer[] }) {
  const [logs, setLogs] = useState<GiftingLog[]>([]); const [form, setForm] = useState<any>(null); const [saving, setSaving] = useState(false); const [error, setError] = useState('')
  const load = () => fetchApi<{ success: boolean; data: GiftingLog[] }>(`/api/influencer-gifting?lineAccountId=${lineAccountId}`).then((res) => setLogs(res.data || [])).catch(() => setError('履歴を取得できませんでした。'))
  useEffect(() => { load() }, [lineAccountId])
  const save = async () => { if (!form.friendId || !form.productName) { setError('クリエイターと商品名を入力してください。'); return }; setSaving(true); setError(''); try { const numeric = ['reach', 'impressions', 'likes', 'comments', 'saves']; const body: any = { ...form, lineAccountId }; numeric.forEach((key) => { body[key] = form[key] === '' ? null : Number(form[key]) }); ['requestedAt', 'shippedAt', 'postPublishedAt'].forEach((key) => { if (!body[key]) body[key] = null }); const res = await fetchApi<{ success: boolean }>(form.id ? `/api/influencer-gifting/${form.id}` : '/api/influencer-gifting', { method: form.id ? 'PATCH' : 'POST', body: JSON.stringify(body) }); if (!res.success) throw new Error(); setForm(null); load() } catch { setError('保存できませんでした。入力内容を確認してください。') } finally { setSaving(false) } }
  return <section className="mt-5 space-y-5"><div className="flex flex-wrap items-center justify-between gap-3 rounded-xl border border-emerald-100 bg-emerald-50 px-5 py-4"><div><p className="text-xs font-semibold tracking-widest text-emerald-700">GIFTING LEDGER</p><h2 className="mt-1 text-lg font-bold">ギフティング履歴・効果測定</h2><p className="mt-1 text-sm text-slate-600">商品ページ、送付、投稿、SNS実績を案件ごとに記録します。</p></div><button onClick={() => setForm({ ...blankLog })} className="rounded-lg bg-emerald-700 px-4 py-2 text-sm font-bold text-white hover:bg-emerald-800">ギフティングを記録</button></div>{error && <p className="text-sm text-red-600">{error}</p>}{form && <div className="rounded-xl border border-slate-200 bg-white p-5"><div className="mb-4 flex items-center justify-between"><h3 className="font-bold">{form.id ? 'ギフティング記録を更新' : '新しいギフティングを記録'}</h3><button onClick={() => setForm(null)} className="text-sm text-slate-500">閉じる</button></div><div className="grid gap-4 md:grid-cols-2"><label className="text-sm font-medium">クリエイター<select value={form.friendId} onChange={(e) => setForm({ ...form, friendId: e.target.value })} className="mt-1 w-full rounded-lg border p-2"><option value="">選択してください</option>{creators.map((creator) => <option key={creator.friendId} value={creator.friendId}>{creator.displayName || '名称未登録'} {creator.instagramHandle ? `(${creator.instagramHandle})` : ''}</option>)}</select></label><label className="text-sm font-medium">商品名<input value={form.productName} onChange={(e) => setForm({ ...form, productName: e.target.value })} className="mt-1 w-full rounded-lg border p-2" /></label><label className="text-sm font-medium">商品ページURL<input value={form.productPageUrl} onChange={(e) => setForm({ ...form, productPageUrl: e.target.value })} className="mt-1 w-full rounded-lg border p-2" /></label><label className="text-sm font-medium">進行状況<select value={form.status} onChange={(e) => setForm({ ...form, status: e.target.value })} className="mt-1 w-full rounded-lg border p-2">{Object.entries(statusLabels).map(([key, label]) => <option key={key} value={key}>{label}</option>)}</select></label><label className="text-sm font-medium">依頼日<input type="date" value={form.requestedAt} onChange={(e) => setForm({ ...form, requestedAt: e.target.value })} className="mt-1 w-full rounded-lg border p-2" /></label><label className="text-sm font-medium">発送日<input type="date" value={form.shippedAt} onChange={(e) => setForm({ ...form, shippedAt: e.target.value })} className="mt-1 w-full rounded-lg border p-2" /></label><label className="text-sm font-medium">投稿日<input type="date" value={form.postPublishedAt} onChange={(e) => setForm({ ...form, postPublishedAt: e.target.value })} className="mt-1 w-full rounded-lg border p-2" /></label><label className="text-sm font-medium">投稿URL<input value={form.postUrl} onChange={(e) => setForm({ ...form, postUrl: e.target.value })} className="mt-1 w-full rounded-lg border p-2" /></label><label className="text-sm font-medium">リーチ<input type="number" min="0" value={form.reach} onChange={(e) => setForm({ ...form, reach: e.target.value })} className="mt-1 w-full rounded-lg border p-2" /></label><label className="text-sm font-medium">いいね数<input type="number" min="0" value={form.likes} onChange={(e) => setForm({ ...form, likes: e.target.value })} className="mt-1 w-full rounded-lg border p-2" /></label><label className="text-sm font-medium md:col-span-2">効果メモ<textarea value={form.effectNotes} onChange={(e) => setForm({ ...form, effectNotes: e.target.value })} className="mt-1 w-full rounded-lg border p-2" rows={2} placeholder="反応、クーポン利用、次回依頼の判断など" /></label></div><button onClick={save} disabled={saving} className="mt-5 rounded-lg bg-emerald-700 px-5 py-2.5 text-sm font-bold text-white disabled:opacity-50">{saving ? '保存中…' : '記録を保存'}</button></div>}<div className="overflow-x-auto rounded-xl border bg-white"><table className="w-full text-sm"><thead className="bg-slate-50 text-left text-slate-500"><tr><th className="p-3">クリエイター</th><th className="p-3">商品</th><th className="p-3">進行状況</th><th className="p-3">投稿日</th><th className="p-3">投稿・効果</th><th className="p-3"></th></tr></thead><tbody>{logs.map((log) => <tr key={log.id} className="border-t"><td className="p-3 font-medium">{log.creatorName || '名称未登録'}<div className="text-xs text-slate-500">{log.instagramHandle || ''}</div></td><td className="p-3">{log.productPageUrl ? <a href={log.productPageUrl} target="_blank" rel="noreferrer" className="text-emerald-700 underline">{log.productName}</a> : log.productName}</td><td className="p-3">{statusLabels[log.status] || log.status}</td><td className="p-3">{log.postPublishedAt || '—'}</td><td className="p-3">{log.postUrl ? <a href={log.postUrl} target="_blank" rel="noreferrer" className="text-emerald-700 underline">投稿を見る</a> : '—'}{log.reach !== null && <div className="mt-1 text-xs text-slate-500">リーチ {log.reach.toLocaleString()}</div>}</td><td className="p-3"><button onClick={() => setForm({ ...blankLog, ...log, productPageUrl: log.productPageUrl || '', requestedAt: log.requestedAt || '', shippedAt: log.shippedAt || '', postPublishedAt: log.postPublishedAt || '', postType: log.postType || '', postUrl: log.postUrl || '', reach: log.reach ?? '', impressions: log.impressions ?? '', likes: log.likes ?? '', comments: log.comments ?? '', saves: log.saves ?? '', effectNotes: log.effectNotes || '' })} className="text-sm text-emerald-700">編集</button></td></tr>)}</tbody></table>{!logs.length && <p className="p-8 text-center text-slate-500">まだギフティング履歴はありません。「ギフティングを記録」から追加してください。</p>}</div></section>
}

export default function InfluencersPage() {
  const { selectedAccountId, selectedAccount, loading: accountsLoading } = useAccount()
  const [items, setItems] = useState<Influencer[]>([])
  const [query, setQuery] = useState('')
  const [loading, setLoading] = useState(false)
  const [error, setError] = useState('')
  const [selected, setSelected] = useState<Influencer | null>(null)
  const [tab, setTab] = useState<'profiles' | 'history'>('profiles')
  useEffect(() => {
    if (!selectedAccountId) return
    setLoading(true); setError('')
    const q = new URLSearchParams({ lineAccountId: selectedAccountId }); if (query) q.set('q', query)
    fetchApi<{ success: boolean; data: Influencer[] }>(`/api/influencers?${q}`).then((res) => {
      const next = res.data || []
      setItems(next)
      setSelected((current) => current ? next.find((item) => item.friendId === current.friendId) || null : null)
    }).catch(() => setError('一覧を取得できませんでした。権限または接続を確認してください。')).finally(() => setLoading(false))
  }, [selectedAccountId, query])
  const complete = useMemo(() => items.filter((item) => item.profileCompletedAt).length, [items])
  if (accountsLoading) return <div className="p-8">読み込み中…</div>
  if (!selectedAccountId) return <div className="p-8">利用できるLINEアカウントがありません。</div>
  return <main className="p-6 max-w-6xl mx-auto">
    <div className="flex flex-wrap items-end justify-between gap-4 mb-7"><div><p className="text-xs font-semibold tracking-widest text-emerald-700">CREATOR GIFTING</p><h1 className="text-2xl font-bold mt-1">インフルエンサー管理</h1><p className="text-sm text-gray-500 mt-1">{selectedAccount?.displayName || selectedAccount?.name} のプロフィール登録・進行管理</p></div><div className="flex items-center gap-3"><button type="button" onClick={() => downloadInfluencers(items)} disabled={!items.length} className="rounded-xl border border-emerald-300 bg-white px-4 py-3 text-sm font-medium text-emerald-800 hover:bg-emerald-50 disabled:cursor-not-allowed disabled:opacity-50">Excelをダウンロード</button><div className="rounded-xl bg-emerald-50 px-4 py-3 text-sm text-emerald-900">登録済み <b className="text-lg">{complete}</b> / {items.length} 名</div></div></div>
    <div className="mb-5 flex gap-2 border-b"><button onClick={() => setTab('profiles')} className={`border-b-2 px-4 py-2 text-sm font-bold ${tab === 'profiles' ? 'border-emerald-600 text-emerald-700' : 'border-transparent text-slate-500'}`}>クリエイター一覧</button><button onClick={() => setTab('history')} className={`border-b-2 px-4 py-2 text-sm font-bold ${tab === 'history' ? 'border-emerald-600 text-emerald-700' : 'border-transparent text-slate-500'}`}>ギフティング履歴・効果測定</button></div>
    {tab === 'history' ? <GiftingHistory lineAccountId={selectedAccountId} creators={items} /> : <><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="名前・Instagramアカウントで検索" className="w-full max-w-md border rounded-lg px-3 py-2 mb-5" />
    {error && <p className="text-red-600 mb-4">{error}</p>}
    <div className="overflow-x-auto bg-white border rounded-xl"><table className="w-full text-sm"><thead className="bg-gray-50 text-left text-gray-500"><tr><th className="p-3">クリエイター</th><th className="p-3">ジャンル</th><th className="p-3">フォロワー数</th><th className="p-3">連絡先</th><th className="p-3">発送先</th><th className="p-3">状態</th></tr></thead><tbody>{items.map((item) => <tr key={item.friendId} onClick={() => setSelected(item)} className={`cursor-pointer border-t transition hover:bg-emerald-50 ${selected?.friendId === item.friendId ? 'bg-emerald-50' : ''}`}><td className="p-3 font-medium">{item.displayName || '名称未登録'}<div className="text-xs font-normal text-gray-500">{item.instagramHandle || 'Instagram未登録'}</div></td><td className="p-3">{item.categories.join('・') || '—'}</td><td className="p-3">{item.followerBand || '—'}</td><td className="p-3">{item.contactEmail || item.contactPhone || '—'}</td><td className="p-3">{item.address?.prefecture || '未登録'}</td><td className="p-3"><span className={`rounded-full px-2 py-1 text-xs ${item.profileCompletedAt ? 'bg-emerald-100 text-emerald-800' : 'bg-amber-100 text-amber-800'}`}>{item.profileCompletedAt ? '登録済み' : '未登録'}</span></td></tr>)}</tbody></table>{!loading && !items.length && <p className="p-8 text-center text-gray-500">まだプロフィール登録者はいません。</p>}</div>
    {selected && <section className="mt-6 overflow-hidden rounded-xl border border-emerald-200 bg-white shadow-sm"><div className="flex items-start justify-between gap-4 border-b border-emerald-100 bg-emerald-50 px-5 py-4"><div><p className="text-xs font-semibold tracking-widest text-emerald-700">CREATOR RECORD</p><h2 className="mt-1 text-xl font-bold">{selected.displayName || '名称未登録'} <span className="font-normal text-slate-500">{selected.instagramHandle || ''}</span></h2></div><button onClick={() => setSelected(null)} className="rounded-lg border border-emerald-200 bg-white px-3 py-1.5 text-sm text-emerald-800 hover:bg-emerald-100">閉じる</button></div><div className="grid gap-6 p-5 lg:grid-cols-2"><div><h3 className="mb-4 border-l-4 border-emerald-500 pl-2 text-sm font-bold">プロフィール・連絡先</h3><dl className="grid grid-cols-2 gap-x-6 gap-y-4"><Field label="Instagramアカウント" value={selected.instagramHandle} /><Field label="フォロワー数" value={selected.followerBand} /><Field label="発信ジャンル" value={selected.categories.join('・')} /><Field label="公式ショップ購入経験" value={selected.hasShopifyPurchase ? 'あり' : 'なし'} /><Field label="LINEの状態" value={selected.isFollowing ? '登録中' : 'ブロック中'} /><Field label="メールアドレス" value={selected.contactEmail} /><Field label="連絡先電話番号" value={selected.contactPhone} /><Field label="年代" value={selected.ageGroup} /><Field label="性別" value={selected.gender} /><Field label="興味のあるギフティング" value={selected.giftingInterests.join('・')} /><Field label="アレルギー・避けたい食材" value={selected.dietaryNotes} /></dl></div><div className="rounded-lg border border-amber-200 bg-amber-50 p-4"><h3 className="mb-4 border-l-4 border-amber-500 pl-2 text-sm font-bold text-amber-950">配送先情報</h3>{selected.address ? <dl className="grid grid-cols-2 gap-x-6 gap-y-4"><Field label="お名前" value={selected.address.recipientName} /><Field label="配送先電話番号" value={selected.address.phone} /><Field label="郵便番号" value={selected.address.postalCode} /><Field label="都道府県" value={selected.address.prefecture} /><div className="col-span-2"><Field label="市区町村・町名・番地" value={selected.address.addressLine1} /></div><div className="col-span-2"><Field label="建物名・部屋番号" value={selected.address.addressLine2} /></div></dl> : <p className="text-sm text-amber-900">配送先はまだ登録されていません。</p>}</div></div></section>}</>}
  </main>
}
